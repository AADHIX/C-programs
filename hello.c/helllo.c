#include <stdio.h>

int main() 
{

    int a,n,i;
    for ( i = 0; i < n; i++)
    {
        /* code */
    
    
    printf("Enter the  limit f numbers!\n");
    scanf("%d", &n);
    printf("Enter the numbers",i++);
    }
    return 0;
}